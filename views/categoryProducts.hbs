<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Products in "{{category.name}}" Category</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .product-info {
      text-align: center;
    }
    .product-info h4 {
      margin-bottom: 0.5rem;
    }
    .product-info p.price {
      margin-bottom: 1rem;
      font-size: 1.25rem;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <div class="container my-5">
    <h1 class="text-center mb-5">Products in "{{category.name}}" Category</h1>
    <div class="row">
      {{#each products}}
      <div class="col-lg-3 col-md-4 col-sm-6 mb-4">
        <div class="block">
          <img class="img-fluid" src="{{this.imageUrl}}" alt="{{this.name}}">
        </div>
        <div class="product-info">
          <h4 class="mb-2">
            <a href="/product/{{this._id}}" class="link-title text-decoration-none">{{this.name}}</a>
          </h4>
          <p class="price">${{this.price}}</p>
        </div>
      </div>
      {{/each}}
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
