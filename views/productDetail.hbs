<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{product.name}}</title>
  <!-- Liên kết đến Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Liên kết đến tùy chỉnh CSS của bạn nếu cần -->
  <style>
    .product-image {
      border-radius: 12px;
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
      transition: transform 0.3s ease;
    }
    .product-image:hover {
      transform: scale(1.05);
    }
    .product-details {
      padding: 20px;
      border-radius: 12px;
      background-color: #f8f9fa;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }
    .price {
      font-size: 1.8rem;
      font-weight: bold;
      color: #28a745;
    }
    .old-price {
      text-decoration: line-through;
      color: #6c757d;
    }
    .btn-add-to-cart {
      background-color: #007bff;
      border-color: #007bff;
    }
    .btn-add-to-cart:hover {
      background-color: #0056b3;
      border-color: #004085;
    }
  </style>
</head>
<body>
  <div class="container my-5">
    <div class="row">
      <!-- Hình ảnh sản phẩm -->
      <div class="col-md-6 mb-4 mb-md-0">
        <div class="d-flex justify-content-center">
          <img class="img-fluid product-image" src="{{product.imageUrl}}" alt="{{product.name}}" />
        </div>
      </div>
      <!-- Thông tin sản phẩm -->
      <div class="col-md-6">
        <div class="product-details">
          <h1 class="mb-3">{{product.name}}</h1>
          <p class="lead">{{product.description}}</p>
          <p class="price">
            ${{product.price}}
            {{#if product.originalPrice}}
            <span class="old-price ms-2">${{product.originalPrice}}</span>
            {{/if}}
          </p>
          <p class="mb-4"><strong>Stock:</strong> {{product.stock}}</p>
          <button class="btn btn-add-to-cart btn-lg mb-4"
                  data-item-id="{{product.id}}"
                  data-item-name="{{product.name}}"
                  data-item-image="{{product.imageUrl}}"
                  data-item-price="{{product.price}}"
                  data-item-url="/product/{{product.id}}"
                  data-item-custom1-name="Choose Color"
                  data-item-custom1-options="{{product.colors}}"
                  data-item-custom2-name="Choose Size"
                  data-item-custom2-options="{{product.sizes}}">
            Add to Cart
          </button>
        </div>
      </div>
    </div>
  </div>

  <!-- Liên kết đến Bootstrap JS và các phụ thuộc nếu cần -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
